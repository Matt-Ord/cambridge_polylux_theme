{
    "name": "Debian",
    "image": "mcr.microsoft.com/devcontainers/base:bookworm",
    "features": {
        "ghcr.io/devcontainers/features/rust:1": {
            "version": "latest",
            "profile": "default"
        }
    },
    "customizations": {
        "vscode": {
            "settings": {
                "editor.formatOnSave": true,
                "editor.codeActionsOnSave": {
                    "source.fixAll": "always"
                }
            },
            "extensions": [
                "myriad-dreamin.tinymist"
            ]
        }
    },
    "onCreateCommand": "sudo apt-get update && sudo apt-get install fontconfig pkg-config -y && cargo install --locked typst-cli && cargo install typship && sudo mkdir -p /usr/share/fonts/custom && sudo cp fonts/* /usr/share/fonts/custom/ && sudo fc-cache -fv"
}